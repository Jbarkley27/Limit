using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AimAssistance : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // Convert world position to screen space
        //Vector3 screenPos = Camera.main.WorldToScreenPoint(worldObject.position);

        //// Check if the screen position is within the bounds of the UI object
        //if (RectTransformUtility.RectangleContainsScreenPoint(uiObject, screenPos))
        //{
        //    Debug.Log("World object is within the bounds of the UI object.");
        //}
        //else
        //{
        //    Debug.Log("World object is outside the bounds of the UI object.");
        //}
    }
}
